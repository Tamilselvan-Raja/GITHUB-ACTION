name: Hello world
on: 
  push: 
    branches: 
      - main
      - dev
      - 'release/**'
  pull_request:
    types:
      - opened
    branches:
      - mains
jobs:
 build:
   runs-on: ubuntu-latest
   steps:
    - name: Build step 
      run: echo "A action build"